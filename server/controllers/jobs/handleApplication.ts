import { Response } from "express";
import { IUserRequest } from "../../types";

export const handleApplication = async (req:IUserRequest, res:Response) => {
const {id} = req.user
    try{
        
    }catch(err){

    }


}